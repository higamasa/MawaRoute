<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/blue_dark</item>
        <item name="colorAccent">@color/orenge</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.BaseToolbarTheme" parent="@style/Widget.AppCompat.Toolbar">
        <item name="android:theme">@style/AppTheme.ToolbarTheme</item>
    </style>

    <style name="AppTheme.ToolbarTheme" parent="AppTheme.NoActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/darker_gray</item>
    </style>

    <style name="searchItem">
        <item name="android:textColorHint">@color/colorWhite</item>
        <item name="android:editTextColor">@color/colorWhite</item>
    </style>

    <style name="Animation">
        <item name="android:windowEnterAnimation">@animator/anim</item>
        <item name="android:windowExitAnimation">@animator/anim_hide</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
